# Application configuration variables in Ansible-style format - Generated by Ansible
APP_CONFIG = {
    "title": "{{ app_title }}",
    "subtitle": "{{ app_subtitle }}",
    "icon": "{{ app_icon }}",
    "theme": {
        "primary_color": "{{ theme.primary_color }}",
        "secondary_color": "{{ theme.secondary_color }}",
        "background_color": "{{ theme.background_color }}",
        "secondary_background_color": "{{ theme.secondary_background_color }}"
    },
    "api": {
        "base_url": "{{ api_base_url }}",
        "endpoints": {
            "embed": "{{ api_endpoints.embed }}",
            "index": "{{ api_endpoints.index }}",
            "search": "{{ api_endpoints.search }}"
        }
    },
    "status": {
        "embedding_engine": "Online",
        "vector_database": "Connected",
        "api": "Operational"
    }
}